extends Node2D

var d = 0.0
var radius =250 
var speed = 10.0
var parent_rotation
var circle

# Called when the node enters the scene tree for the first time.
func _ready():
	pass


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
#	if Input.is_action_pressed("ui_up"):
#
#		d += delta
#		position = Vector2(
#			sin(d * speed) * get_global_mouse_position().x,
#			cos(d * speed) * get_global_mouse_position().y
#		) 
#	if Input.is_action_pressed("ui_down"):
#
#		d -= delta
#		position = Vector2(
#			sin(d * speed) * radius,
#			cos(d * speed) * radius 
#		) 

		
		
#	if Input.is_action_pressed("ui_up"):
#
#		d += delta
#		position = Vector2(
#			sin(d * speed) * radius,
#			cos(d * speed) * radius 
#		) 
#	if Input.is_action_pressed("ui_down"):
#
#		d -= delta
#		position = Vector2(
#			sin(d * speed) * radius,
#			cos(d * speed) * radius 
#		) 
